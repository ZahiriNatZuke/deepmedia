$(document).ready(function () {
  const player = $('#video-player');
  const controlBar = $('.control-bar');
  const volumenBtn = $('#volumen-btn');
  const volumenControl = $('#volumen-control');
  const volumenSlider = $('#volumen-slider');
  const progressBarVideo = $('#progress-bar-video');
});


